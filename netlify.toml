[build]
  command = "npm install && npm run build"
  functions = "netlify/functions"
  publish = "public" # Create empty folder

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server/:splat"
  status = 200